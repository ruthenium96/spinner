How to build project:
 * install docker
 * `chmod +x docker.sh`
 * `sudo ./docker.sh init`
 * `sudo ./docker.sh shell` or `sudo ./docker.sh shell_restart`
 * `cd project`
 * `mkdir build`
 * `cd build`
 * `cmake ..`
 * `make` or `cmake --build .`